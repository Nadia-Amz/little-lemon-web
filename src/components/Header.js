import React from "react";
import logo from "../images/logo.jpg";

const Header = () => {
    return(
        <section>
        <img src= {logo} alt=""></img>
        </section>
        
    );
};
export default Header;
